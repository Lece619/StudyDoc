DML
	SELECT
	INSERT
 	UPDATE
	DELETE

조건식 : 참 또는 거짓 둘 중 하나의 결과가 나오는 식

-WHERE 조건식
- >,<	: 초과 미만
- >=,<=	: 이상 이하
- =	: 같다
- <>,!=,^=: 같지않다
- AND : 두 조건식 모두 참이면 참
- OR : 둘 중 하나라도 참이면 참
NOR?


--DELETE와 TRUNCATE 
--DELETE 는 복구가 가능하다.
--TRUNCATE는 복구가 불가능하다.